# inline format test
format X:
    len :u8
    inline :format:
        x :u8
    named_inline: [len]format Item:
        config.type = u8
        input.endian = config.endian.little
        hello :u16
        hello == 2
        if hello == 0:
            error("oh no")
    if len == 0:
        why :u16
    reuse :Item
    inline.x == 0

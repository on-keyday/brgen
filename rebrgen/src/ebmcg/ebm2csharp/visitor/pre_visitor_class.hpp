/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/brgen
/*
  Name: pre_visitor_class
  Available Variables:
    ctx: Context_pre_visitor
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      ebm: ebm::ExtendedBinaryModule&
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/


#include <set>
#include <string>
#include "../codegen.hpp"

inline bool is_csharp_reserved(const std::string& name) {
    static const std::set<std::string> reserved = {
        // keywords (C# language specification 6.4.4)
        "abstract", "as", "base", "bool", "break", "byte", "case", "catch",
        "char", "checked", "class", "const", "continue", "decimal", "default",
        "delegate", "do", "double", "else", "enum", "event", "explicit",
        "extern", "false", "finally", "fixed", "float", "for", "foreach",
        "goto", "if", "implicit", "in", "int", "interface", "internal", "is",
        "lock", "long", "namespace", "new", "null", "object", "operator",
        "out", "override", "params", "private", "protected", "public",
        "readonly", "ref", "return", "sbyte", "sealed", "short", "sizeof",
        "stackalloc", "static", "string", "struct", "switch", "this", "throw",
        "true", "try", "typeof", "uint", "ulong", "unchecked", "unsafe",
        "ushort", "using", "virtual", "void", "volatile", "while",
        // contextual keywords that read confusingly as identifiers
        "var", "value", "yield", "record",
    };
    return reserved.contains(name);
}

DEFINE_VISITOR(pre_visitor) {
    using namespace CODEGEN_NAMESPACE;
    // C# puts fields and methods in a single declaration space: a property
    // accessor named after its backing public field (e.g. the constrained
    // vector setter `data(...)` next to field `data`) is CS0102. Prefix all
    // property accessor functions with get_/set_; the modifier runs on every
    // identifier() lookup, so definitions and call sites stay consistent.
    auto* module_ = &ctx.module();
    ctx.module().set_identifier_modifier(
        [module_](ebm::StatementRef ref, std::string& name) {
            if (auto stmt = module_->get_statement(ref)) {
                if (auto fd = stmt->body.func_decl()) {
                    switch (fd->kind) {
                        case ebm::FunctionKind::PROPERTY_GETTER:
                        case ebm::FunctionKind::COMPOSITE_GETTER:
                            name = "get_" + name;
                            break;
                        case ebm::FunctionKind::PROPERTY_SETTER:
                        case ebm::FunctionKind::COMPOSITE_SETTER:
                        case ebm::FunctionKind::VECTOR_SETTER:
                            name = "set_" + name;
                            break;
                        default:
                            break;
                    }
                }
            }
            if (is_csharp_reserved(name)) {
                name += "_";
            }
        });
    return pass;
}


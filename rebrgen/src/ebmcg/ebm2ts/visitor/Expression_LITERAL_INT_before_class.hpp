/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/brgen
/*
  Name: Expression_LITERAL_INT_before_class
  Available Variables:
    ctx: Context_Expression_LITERAL_INT_before<Result>
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      item_id: ebm::ExpressionRef
      type: const ebm::TypeRef&
      kind: const ebm::ExpressionKind&
      int_value: const ebm::Varint&
      main_logic: ebmcodegen::util::MainLogicWrapper<Result>
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/


#include "../codegen.hpp"

// When --use-bigint is set and the literal's typed home is bigint
// (INT/UINT > 32 bits), append the JS `n` suffix so arithmetic with
// surrounding bigint operands doesn't raise "Cannot mix BigInt and
// other types". Without this, a bgn-level `i + 1` lowers to `i + 1`
// where `i` is bigint but the literal stays number.
DEFINE_VISITOR(Expression_LITERAL_INT_before) {
    using namespace CODEGEN_NAMESPACE;
    if (!ctx.flags().use_bigint) {
        return pass;
    }
    MAYBE(t, ctx.get(ctx.type));
    const auto k = t.body.kind;
    if (k != ebm::TypeKind::INT && k != ebm::TypeKind::UINT) {
        return pass;
    }
    std::uint64_t bits = 0;
    if (auto sz = t.body.size()) {
        bits = sz->value();
    }
    if (bits <= 32) {
        return pass;
    }
    return Result(std::format("{}n", ctx.int_value.value()));
}


/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/rebrgen
/*
  Name: pre_visitor_class
  Available Variables:
    ctx: Context_pre_visitor
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      ebm: ebm::ExtendedBinaryModule&
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/


#include "../codegen.hpp"

// Zig primitives that cannot be used as identifiers without escaping
inline bool is_zig_primitive(const std::string& name) {
    // Integer types: u0..u65535, i0..i65535
    if (name.size() >= 2 && (name[0] == 'u' || name[0] == 'i')) {
        bool all_digits = true;
        for (size_t j = 1; j < name.size(); j++) {
            if (!std::isdigit(name[j])) {
                all_digits = false;
                break;
            }
        }
        if (all_digits) return true;
    }
    // Other primitives and keywords
    static const std::unordered_set<std::string> primitives = {
        "bool", "true", "false", "null", "undefined",
        "f16", "f32", "f64", "f80", "f128",
        "usize", "isize", "comptime_int", "comptime_float",
        "anytype", "anyframe", "anyerror", "anyopaque",
        "void", "noreturn", "type", "error",
    };
    return primitives.contains(name);
}

DEFINE_VISITOR(pre_visitor) {
    using namespace CODEGEN_NAMESPACE;

    ctx.module().set_identifier_modifier(
        [](ebm::StatementRef ref, std::string& name) {
            if (is_zig_primitive(name)) {
                name = "@\"" + name + "\"";
            }
        });
    return pass;
}


/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/brgen
/*
  Name: Expression_LITERAL_INT_before_class
  Available Variables:
    ctx: Context_Expression_LITERAL_INT_before<Result>
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      item_id: ebm::ExpressionRef
      type: const ebm::TypeRef&
      kind: const ebm::ExpressionKind&
      int_value: const ebm::Varint&
      main_logic: ebmcodegen::util::MainLogicWrapper<Result>
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/

#include <format>
#include "../codegen.hpp"
#include "ebm/extended_binary_module.hpp"

// Hike types an untyped integer literal as `int` (i64) even where the other
// operand is narrower, and then emits `add i64 6, %v25` with an i8 operand,
// which clang rejects. Spelling the literal's own type keeps both sides the
// same width.
DEFINE_VISITOR(Expression_LITERAL_INT_before) {
    using namespace CODEGEN_NAMESPACE;
    auto kind = ctx.get_kind(ctx.type);
    if (!kind) {
        return pass;
    }
    switch (*kind) {
        case ebm::TypeKind::UINT:
        case ebm::TypeKind::INT:
        case ebm::TypeKind::USIZE:
        case ebm::TypeKind::ENUM:
            break;
        default:
            return pass;
    }
    MAYBE(type_str, ctx.visit(ctx.type));
    return CODE(type_str.to_writer(), "(", std::format("{}", ctx.int_value.value()), ")");
}

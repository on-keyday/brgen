/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/brgen
/*
  Name: Statement_PROGRAM_DECL_before_class
  Available Variables:
    ctx: Context_Statement_PROGRAM_DECL
      visitor: MergedVisitor&
      item_id: ebm::StatementRef
      kind: const ebm::StatementKind&
      block: const ebm::Block&
      main_logic: ebmcodegen::util::MainLogicWrapper<Result>
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/

#include "../codegen.hpp"
#include "ebmcodegen/stub/url.hpp"

DEFINE_VISITOR(Statement_PROGRAM_DECL_before) {
    using namespace CODEGEN_NAMESPACE;

    MAYBE(main, ctx.main_logic());

    // `config.hike.package` in the .bgn metadata, or --config hike.package=NAME,
    // names the package; both fall back to main.
    constexpr auto package_key = "config.hike.package";
    std::string package_name{ctx.flags().get_config(package_key)};
    if (package_name.empty()) {
        MAYBE(meta_res, get_metadata(ctx, ctx.item_id));
        if (auto package_meta = meta_res.get_first(package_key)) {
            if (auto name_res = package_meta->get_string(ctx, 0)) {
                package_name = *name_res;
            }
        }
    }
    if (package_name.empty()) {
        package_name = "main";
    }

    CodeWriter w;
    write_generated_banner(ctx, w);
    w.writeln("package ", package_name);
    w.writeln();
    if (!ctx.flags().no_prelude) {
        w.write(ebm2hike::runtime_prelude);
        w.writeln();
    }
    w.write(main.to_writer());
    return w;
}

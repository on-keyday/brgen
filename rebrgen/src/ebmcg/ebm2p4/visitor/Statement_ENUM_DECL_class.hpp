/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/rebrgen
/*
  Name: Statement_ENUM_DECL_class
  Available Variables:
    ctx: Context_Statement_ENUM_DECL
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      item_id: ebm::StatementRef
      kind: const ebm::StatementKind&
      enum_decl: const ebm::EnumDecl&
        name: IdentifierRef
        base_type: TypeRef
        members: Block
          len: Varint
          container: std::vector<StatementRef>
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/
#include <cstddef>
#include "../codegen.hpp"
DEFINE_VISITOR(Statement_ENUM_DECL) {
    using namespace CODEGEN_NAMESPACE;
    auto name = ctx.identifier();

    // 改善1: base_type の構築ロジックを整理
    // optionalを使わず、デフォルト値を先にセットすることで後の記述をシンプルにする
    CodeWriter base_type_w;
    if (!is_nil(ctx.enum_decl.base_type)) {
        MAYBE(base_type_res, ctx.visit(ctx.enum_decl.base_type));
        base_type_w = CODE(std::move(base_type_res.to_writer()), " ");
    }

    CodeWriter w;
    // 改善2: フォーマット修正 (カンマ後のスペース)
    w.writeln("enum ", base_type_w, name, " {");

    {
        auto scope = w.indent_scope();
        // 改善3: 未使用の is_first_member フラグと分岐を削除
        for (size_t i = 0; i < ctx.enum_decl.members.container.size(); ++i) {
            MAYBE(member_str, ctx.visit(ctx.enum_decl.members.container[i]));
            w.write(member_str.to_writer());
            if (i != ctx.enum_decl.members.container.size() - 1) {
                w.write(",");  // カンマを追加
            }
            w.writeln();
        }
    }
    w.writeln("}");

    return w;
}

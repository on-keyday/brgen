/*DO NOT EDIT BELOW SECTION MANUALLY*/
/*license*/
// Template generated by ebmcodegen at https://github.com/on-keyday/brgen
/*
  Name: Expression_IS_LITTLE_ENDIAN_class
  Available Variables:
    ctx: Context_Expression_IS_LITTLE_ENDIAN
      visitor: MergedVisitor&
        program_name: static constexpr const char*
        flags: Flags&
        output: Output&
        wm: ebmcodegen::WriterManager<CodeWriter>
        module_: ebmgen::MappingTable
      item_id: ebm::ExpressionRef
      type: const ebm::TypeRef&
      kind: const ebm::ExpressionKind&
      endian_expr: const ebm::StatementRef&
*/
/*DO NOT EDIT ABOVE SECTION MANUALLY*/

#include "../codegen.hpp"
DEFINE_VISITOR(Expression_IS_LITTLE_ENDIAN) {
    using namespace CODEGEN_NAMESPACE;
    if (ctx.config().is_little_endian_visitor) {
        MAYBE(res, ctx.config().is_little_endian_visitor(ctx));
        return res;
    }
    if (is_nil(ctx.endian_expr)) {
        // Native endianness: emit the configured runtime check expression.
        if (!ctx.config().native_endian_check.empty()) {
            CodeWriter w;
            w.write(ctx.config().native_endian_check);
            return w;
        }
        // No check configured — fall through to the unimplemented marker.
        return ctx.get_impl<expected<Result>>([&](auto&& impl) {
            return impl.visitor_Expression_IS_LITTLE_ENDIAN_GeneratorDefaultHook.visit(ctx);
        });
    }
    // Dynamic endian: the ENDIAN_VARIABLE pointed to by endian_expr was lowered to a
    // variable declaration whose identifier is associated with that StatementRef.
    // The variable holds an integer where 1 == little-endian (matching ebmgen order_value).
    auto var_name = ctx.identifier(ctx.endian_expr);
    CodeWriter w;
    w.write(var_name, " == ", ctx.config().little_endian_value);
    return w;
}

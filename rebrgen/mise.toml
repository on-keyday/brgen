[tools]
# ebmgen (Core) 用
llvm = "19"          # clang++, clang-format 等を含む
cmake = "3.26"
jq = "1.6"
python = "3.11"      # script/build.py 用（お使いのバージョンに合わせて）

# webplayground / 多言語展開用
go = "1.22"          # API/MCP サーバー用
rust = "1.78"        # Wasm ビルド用
# zig = "0.13.0"     # もし Zig ジェネレータの検証も自動化するなら
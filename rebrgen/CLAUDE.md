# CLAUDE.md

@AGENTS.md

Claude Code 固有の再利用可能な作業手順は `.claude/skills/` にある。タスクに該当するスキルを使用し、その `SKILL.md` を最後まで読んでから作業すること。

Codex CLI 用の `.agents/skills/` は `.claude/skills/` にある同名スキルを参照する薄いアダプターである。知識を更新するときは `.claude/skills/` 側を正本として編集すること。

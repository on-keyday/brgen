//Code generated by json2cpp2
#pragma once
#include <cstdint>
#include <binary/flags.h>
#include <vector>
#include <array>
#include <view/iovec.h>
#include <optional>
#include <binary/number.h>
struct TEST_CLASS {
    ::futils::view::rvec data;
    bool encode(::futils::binary::writer& w) const ;
    bool decode(::futils::binary::reader& r);
};
inline bool TEST_CLASS::encode(::futils::binary::writer& w) const {
    if (!w.write(this->data)) {
        return false;
    }
    return true;
}
inline bool TEST_CLASS::decode(::futils::binary::reader& r) {
    if (r.remain().size() < 0) {
        return false;
    }
    if (!r.read(this->data, (r.remain().size() - 0))) {
        return false;
    }
    return true;
}

